FROM python:3.12-slim

RUN apt-get update && apt-get install -y git git-lfs curl jq && rm -rf /var/lib/apt/lists/*
RUN git lfs install

WORKDIR /app
RUN pip install --no-cache-dir huggingface_hub[hf_transfer] requests

COPY test_large_upload.py /app/
CMD ["python", "test_large_upload.py"]
